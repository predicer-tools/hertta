/*

use hertta::weather_data::get_weather_data;

#[cfg(test)]
mod integration_tests {
    use super::*;

    #[test]
    fn test_weather_data_from_api() {
        let start_time = "2022-04-01 00:00".to_string();
        let end_time = "2022-04-02 00:00".to_string();
        let latlon = "61.4481,23.8521".to_string();

        let data = get_weather_data(start_time, end_time, latlon);

        // Assert that the data matches expected values
        // This depends on the structure of your data and what you know about the expected results
        assert!(!data.is_empty());
        // Add more specific assertions here
    }
}

*/
